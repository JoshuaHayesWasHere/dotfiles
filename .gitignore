# OS
.DS_Store
Thumbs.db

# Editor
*.swp
*.swo
*~
.idea/
.vscode/

# Runtime/cache state inside tracked configs
config/**/*.log
config/**/*.cache
config/nvim/undodir/

# Secrets — never commit, even if an aws/ dir gets added later
aws/credentials
aws/sso/
aws/cli/
**/.env
**/*.pem
**/*.key
